---
"@mirasen/react-chessboard": minor
---

Widen `checkSquare` prop type to accept `ColorInput` (e.g. `'w'`, `'white'`) in addition to `SquareString | null`, delegating king-location to the core check extension. Updates minimum `@mirasen/chessboard` peer to `^1.5.0`.
